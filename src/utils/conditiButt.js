
export const handleClick = (name,array) => {

if (name=== 'weapons') {
 return array.weapons
}if (name=== 'shields') {
    return array.shields
} if (name=== 'armors') {
    return array.armors
}
   
}
